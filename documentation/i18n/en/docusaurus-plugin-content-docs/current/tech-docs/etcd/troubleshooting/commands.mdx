---
id: commands
---

import { EtcdCommandList } from '@site/src/components/commonBlocks'

# 5.1.5.3. Useful Commands

<quote>
  To use the executable on the master, we will use the `etcdctl` alias; to use etcdctl
  inside a container, use the `ectl`, `nectl` aliases.
</quote>

<EtcdCommandList />
