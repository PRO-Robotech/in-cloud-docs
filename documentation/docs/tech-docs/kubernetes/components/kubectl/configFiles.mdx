import dedent                   from 'ts-dedent'
import CodeBlock                from '@theme/CodeBlock'
import { CUSTOM_VALUE }         from '@site/src/constants/kubernetes/customValue'


<h4>Переменные окружния</h4>
<CodeBlock language="bash">
    {dedent`
        export BASE_K8S_PATH="${CUSTOM_VALUE.kuberneteBaseFolderPath.value}"
        export KUBECONFIG=${CUSTOM_VALUE.kuberneteBaseFolderPath.value}/admin.conf
    `}
</CodeBlock>

:::warning
Чтобы получить доступ к кластеру с мастер-ноды, необходимо использовать переменную окружения `KUBECONFIG` или разместить файл конфигурации в каталоге `~/.kube/config`.
:::
