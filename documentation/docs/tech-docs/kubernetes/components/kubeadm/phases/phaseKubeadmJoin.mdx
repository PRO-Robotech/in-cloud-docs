
```text
root@compute:/home/user# kubeadm join phase

*****
Available Commands:
  control-plane-join    Join a machine as a control plane instance
  control-plane-prepare Prepare the machine for serving a control plane
  kubelet-start         Write kubelet settings, certificates and (re)start the kubelet
  preflight             Run join pre-flight checks
  wait-control-plane    EXPERIMENTAL: Wait for the control plane to start
```

<div className="scrollable-x">
  <table>
    <thead>
      <tr>
        <th>Путь</th>
        <th>Комментарий</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>`$control-plane-join.all`</td>
        <td>Выполняет все шаги присоединения узла к control plane</td>
      </tr>
      <tr>
        <td>`$control-plane-join.etcd`</td>
        <td>Регистрирует новый узел как участник etcd-кластера</td>
      </tr>
      <tr>
        <td>`$control-plane-join.mark-control-plane`</td>
        <td>Помечает узел как control plane (labels и taints)</td>
      </tr>
      <tr>
        <td>`$control-plane-prepare.all`</td>
        <td>Выполняет все подготовительные шаги для control plane</td>
      </tr>
      <tr>
        <td>`$control-plane-prepare.control-plane`</td>
        <td>Генерирует манифесты статических подов для компонентов control plane</td>
      </tr>
      <tr>
        <td>`$control-plane-prepare.certs`</td>
        <td>Генерирует сертификаты для присоединяемого узла</td>
      </tr>
      <tr>
        <td>`$control-plane-prepare.download-certs`</td>
        <td>Загружает общие сертификаты из кластера</td>
      </tr>
      <tr>
        <td>`$control-plane-prepare.kubeconfig`</td>
        <td>Генерирует kubeconfig-файлы для присоединяемого control plane</td>
      </tr>
      <tr>
        <td>`$kubelet-start` </td>
        <td>Настраивает и перезапускает kubelet</td>
      </tr>
      <tr>
        <td>`$preflight`</td>
        <td>Выполняет предварительные проверки перед присоединением</td>
      </tr>
    </tbody>
  </table>
</div>
