import dedent from 'ts-dedent'
import CodeBlock from '@theme/CodeBlock'
import { COMPONENTS_VERSION } from '@site/src/constants/kubernetes/componentsVersion'

<CodeBlock language="bash">
  {dedent`
    journalctl -t kubeadm-installer
  `}
</CodeBlock>

:::note Вывод команды

<CodeBlock language="text">
  {dedent`
    ***** [INFO] Checking current kubeadm version...
    ***** [INFO] Current: none, Target: ${COMPONENTS_VERSION.kubernetes.value}
    ***** [INFO] Download URL: https://*******
    ***** [INFO] Updating kubeadm to version ${COMPONENTS_VERSION.kubernetes.value}...
    ***** [INFO] Working directory: /tmp/tmp.*****
    ***** [INFO] Downloading kubeadm...
    ***** [INFO] Downloading checksum file...
    ***** [INFO] Verifying checksum...
    ***** [INFO] Extracting files...
    ***** [INFO] Installing binaries...
    ***** [INFO] kubeadm successfully updated to ${COMPONENTS_VERSION.kubernetes.value}.
  `}
</CodeBlock>
:::

<CodeBlock language="bash">
  {dedent`
    ls -la /usr/local/bin/ | grep 'kubeadm$'
  `}
</CodeBlock>

:::note Вывод команды:::info

<CodeBlock language="bash">
  {dedent`
    -rwxr-xr-x  1 root root  50253976 Aug 14  2024 kubeadm
  `}
</CodeBlock>
:::
