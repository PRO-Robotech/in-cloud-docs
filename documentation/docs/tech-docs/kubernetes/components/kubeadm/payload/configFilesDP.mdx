import JoinConfigDP from '@site/docs/tech-docs/kubernetes/components/kubeadm/configs/joinConfigDP.mdx'

> Конфигурация `kubeadm` для worker-узла описывает параметры `JoinConfiguration` (bootstrap-токен, apiServerEndpoint). Используется при подключении воркера к существующему кластеру.

<details open className="custom-gray-block">
  <summary>Конфигурация Kubeadm</summary>
  <JoinConfigDP />
</details>
