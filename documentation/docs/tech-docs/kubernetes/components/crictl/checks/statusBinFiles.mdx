import dedent from 'ts-dedent'
import CodeBlock from '@theme/CodeBlock'
import { COMPONENTS_VERSION } from '@site/src/constants/kubernetes/componentsVersion'

<CodeBlock language="bash">
  {dedent`
    journalctl -t crictl-installer
  `}
</CodeBlock>

:::note Вывод команды

<CodeBlock language="text">
  {dedent`
    ***** [INFO] Checking current crictl version...
    ***** [INFO] Current: none, Target: ${COMPONENTS_VERSION.crictl.value}
    ***** [INFO] Download URL: https://*******
    ***** [INFO] Updating crictl to version ${COMPONENTS_VERSION.crictl.value}...
    ***** [INFO] Working directory: /tmp/tmp.*****
    ***** [INFO] Downloading crictl...
    ***** [INFO] Downloading checksum file...
    ***** [INFO] Verifying checksum...
    ***** [INFO] Installing crictl...
    ***** [INFO] crictl successfully updated to ${COMPONENTS_VERSION.crictl.value}.
  `}
</CodeBlock>
:::

<CodeBlock language="bash">
  {dedent`
    ls -la /usr/local/bin/ | grep 'crictl$'
  `}
</CodeBlock>

:::note Вывод команды

<CodeBlock language="bash">
  {dedent`
    -rwxr-xr-x  1 1001  127  58376628 Apr 18  2024 crictl
  `}
</CodeBlock>
:::
