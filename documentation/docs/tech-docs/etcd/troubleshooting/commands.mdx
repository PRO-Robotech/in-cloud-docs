---
id: commands
---

import { EtcdCommandList } from '@site/src/components/commonBlocks'

# 5.1.5.3. Полезные команды

<quote>
  Для использования исполняемого файла на мастере будем использовать псевдоним `etcdctl`, для использования etcdctl
  внутри контейнера, используем псевдонимы `ectl`, `nectl`.
</quote>

<EtcdCommandList />
