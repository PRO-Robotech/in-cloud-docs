---
id: howItWork
---

TLS сертификаты обеспечивают **защищённое соединение** между клиентом и сервером, используя **шифрование, проверку подлинности и контроль целостности данных**. Если используется **mTLS (Mutual TLS)**, проверка подлинности выполняется **в обоих направлениях** – клиент и сервер должны предоставить действующие сертификаты.

---

### 🔗 1. Установление соединения (TLS Handshake)  
- **Клиент отправляет** приветственное сообщение (**Client Hello**) с поддерживаемыми версиями TLS и наборами шифров.  
- **Сервер отвечает** (**Server Hello**), выбирая **наиболее безопасный алгоритм** и отправляя **свой сертификат**.  
- **В mTLS сервер дополнительно запрашивает сертификат клиента** для двусторонней аутентификации.  

---

### ✅ 2. Проверка подлинности (Аутентификация)  
**В стандартном TLS:**  
- Клиент проверяет **сертификат сервера**, удостоверяясь, что он:  
  - **Действителен** и не истёк.  
  - Подписан **доверенным центром сертификации (CA)**.  
  - Соответствует **запрашиваемому домену**.  

**В mTLS:**  
- **Сервер также проверяет сертификат клиента**.  
- Если сертификат клиента подписан доверенным CA, соединение продолжается.  

📌 **mTLS необходим в высокозащищённых системах, таких как API, микросервисы и VPN, где требуется двухсторонняя аутентификация.**  

---

### 🔐 3. Генерация ключа шифрования  
После успешной аутентификации клиент и сервер договариваются о **секретном ключе сессии**, который будет использоваться для симметричного шифрования данных.  

Для этого применяются алгоритмы:  
- **RSA** – асимметричное шифрование (устаревший метод, не рекомендуется).  
- **ECDHE** – современный метод, поддерживающий **PFS**.  

:::note  
**PFS (Perfect Forward Secrecy)** гарантирует, что **каждая сессия использует уникальный ключ**, который удаляется после завершения соединения. Это защищает прошлые сессии, даже если приватный ключ сервера будет скомпрометирован.  
:::

---

### 🔄 4. Обмен зашифрованными данными  
После завершения **TLS Handshake** начинается передача данных:  

- Клиент и сервер **шифруют данные** с использованием **симметричных алгоритмов** (AES, ChaCha20).  
- **mTLS гарантирует**, что передача данных происходит **только между аутентифицированными сторонами**, предотвращая атаки MITM (Man-in-the-Middle).  

:::note  
Каждое сообщение сопровождается **MAC (Message Authentication Code)**, который проверяет целостность данных и предотвращает их подмену.  
:::

---

📌 **TLS защищает данные в сети, а mTLS обеспечивает строгую взаимную проверку доверенных сторон!** 🚀
