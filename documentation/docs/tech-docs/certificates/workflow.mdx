---
id: workflow
---

Создание сертификата — это несколько шагов, которые гарантируют его безопасность и подлинность.  

---
### 🔑 Генерация приватного ключа  
Приватный ключ нужен для подписи и расшифровки данных. Без него сертификат бесполезен.

---
### 📄 Создание запроса на подпись (CSR)  
> **CSR (Certificate Signing Request)** — это **запрос на подпись сертификата**, содержащий информацию о будущем сертификате. Он создаётся владельцем сертификата и отправляется в **удостоверяющий центр (CA)** для проверки и подписи.  

:::note CSR включает в себя:
- **🆔 CN (Common Name)** – имя владельца.    
- **🔧 Usages** – допустимые сценарии использования (clientAuth, serverAuth, etc).  
- **🌍 Атрибуты** – страна (C), организация (O), подразделение (OU).  
- **🔗 SANs** – альтернативные имена (домены, IP-адреса).
- **🔑 Публичный ключ** — используется для шифрования.  
:::

📌 Если сертификат — это паспорт, то CSR — это заполненная анкета на его получение. 

---

### 🔏 Подписание сертификата CA  

CA (удостоверяющий центр) проверяет **CSR**, удостоверяется в его подлинности и подписывает, создавая **полноценный сертификат**.  

После подписания **пара "приватный + публичный ключ"** может использоваться для настройки **TLS, аутентификации и шифрования данных** в различных приложениях. 🚀  
